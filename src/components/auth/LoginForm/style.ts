import styled from "@emotion/styled";

export const FormContainer = styled.div`
  width: 100%;

  display: flex;
  flex-direction: column;
  gap: 1.75rem;

  > div {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }
`;
